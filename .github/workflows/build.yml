on:
  push:
    branches:
      - master
    paths:
      - "config/*.keymap"
      - "config/**/*.dtsi"
      - "config/**/*.h"
      - "config/**/*.conf"
      - "config/**/*.yml"
      - "west.yml"
      - "build.yaml"
  pull_request:
    branches:
      - master
  workflow_dispatch:

name: Corne_ZMK_build

jobs:
  build:
    uses: zmkfirmware/zmk/.github/workflows/build-user-config.yml@main
